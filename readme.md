
Prompt to create gemini.md:
Create gemini.md that contains rules for react typescript application. Use bootstrap.

Prompt to create plan:
Plan a diary react application with spring boot backend. Use spring boot authorization server for authentication. Use bootstrap. Use frontend-maven-plugin. Build a multimodule maven project with dairy-frontend and diary-backend and oauth-server modules. Use latest lts versions of java, spring boot and node. Create unit tests and integration tests. Use selenium page object model to test user interface. Use gemini.md for project rules.