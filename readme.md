
Prompt to create gemini.md:

Create gemini.md that contains rules for react typescript application. 
* Use bootstrap.

Prompt to create plan:

Plan a diary react application with following rules:
* Use react in diary-frontend.
* Use spring boot in diary-backend. 
* Build a multimodule maven project with dairy-frontend and diary-backend and oauth-server modules. 
* Use gemini.md for project rules.
* Use spring boot authorization server for authentication. 
* Create docker image from oauth-server using jib plugin.
* Load oauth-server in diary-backend using testcontainers.
* Use bootstrap. 
* Use frontend-maven-plugin.
* Use webjars to load diary-frontend in diary-backend. 
* Use latest lts versions of java, spring boot and node. 
* Create unit tests and integration tests.
* Use restassured to test diary-backend.
* Use selenium page object model to test user interface.
* Use selenium page object model in diary-backend to test user interface.
